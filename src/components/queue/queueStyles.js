import {makeStyles} from '@mui/styles';

export const useStyles = makeStyles(() => ({
  song: {
    listStyleType: 'none',
    display: 'flex',
  },
  loading: {
    height: '48px',
    borderRadius: '13px',
  },
}));
